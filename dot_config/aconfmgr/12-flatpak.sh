# Flatpak and its packages.

# TODO: We should try to actually manage Flatpak installations, rather than having to manually track them.
AddPackage flatpak # Linux application sandboxing and distribution framework (formerly xdg-app)

# Installed: Vesktop, Spotify
